document.addEventListener('DOMContentLoaded', () => {

    const grid = document.getElementById('batchList');

    if (!grid) {
        return;
    }

    // Por enquanto só garante que a nova grade existe.
    // Nos próximos passos vamos ligar essa grade ao backend.

    console.log('Dashboard V2 carregado.');

});
